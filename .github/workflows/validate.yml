name: Validate project

on:
  push:
    branches:
      - main
      - "agent/**"
  pull_request:

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: agent/dismiss-stale-recovery
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Apply recovery banner fix
        run: node scripts/apply-recovery-banner-fix.mjs

      - name: Verify project and standalone build
        run: npm run verify

      - name: Commit recovery banner fix
        run: |
          git restore --source=HEAD -- .github/workflows/validate.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm scripts/apply-recovery-banner-fix.mjs
          git add -A
          git commit -m "Dismiss stale recovery offers when opening a workspace"
          git push origin HEAD:agent/dismiss-stale-recovery

import { readFile, writeFile } from "node:fs/promises";

const cssPath = "src/styles/app.css";
let css = await readFile(cssPath, "utf8");

const marker = "/* ---------- compact localized header revision ---------- */";
if (css.includes(marker)) throw new Error("Compact header revision already applied.");

css += `\n\n${marker}\nheader{\n  display:flex;align-items:center;gap:8px 12px;padding:10px 16px;flex-wrap:wrap;\n}\nheader>.brand{flex:0 1 auto;min-width:0;margin-inline-end:auto}\nheader>.grow{display:none}\nheader>select.uilang{flex:0 0 auto;width:auto;min-width:118px;max-width:220px}\nheader>button{flex:0 0 auto;width:auto;max-width:none;white-space:nowrap}\nheader>.meter{flex:0 1 300px;width:auto;min-width:210px;max-width:340px;margin-inline-start:4px}\n.filebar{\n  order:10;flex:1 1 100%;width:100%;min-width:0;display:flex;align-items:center;\n  gap:8px;flex-wrap:nowrap;justify-content:flex-start;overflow-x:auto;overflow-y:hidden;\n  padding-block:2px 3px;scrollbar-width:thin;overscroll-behavior-inline:contain;\n}\n.filebar>*{flex:0 0 auto}\n.filebar .btn{white-space:nowrap;overflow:visible;overflow-wrap:normal;min-height:34px}\n.filebar .savepill{width:auto;min-width:170px;max-width:260px;flex:0 0 auto}\n.filebar .fname{flex:1 0 220px;min-width:190px;max-width:420px;margin-inline-start:auto}\n.filebar .fname input{width:auto;min-width:110px;flex:1 1 150px}\n#btnEnRef,#btnSaveJson,#btnLoadJson,#btnNew,#btnExport{width:auto;min-width:max-content;flex:0 0 auto}\n@media (max-width:900px){\n  header{padding:9px 12px}\n  header>.meter{order:5;flex:1 1 100%;max-width:none;margin-inline-start:0}\n  .filebar{order:10}\n}\n@media (max-width:560px){\n  header{gap:7px 8px;padding:8px 10px}\n  header>.brand .sub{display:none}\n  header>select.uilang{min-width:104px;max-width:46vw}\n  header>button{padding:6px 9px}\n  header>.meter{min-width:0}\n  .filebar .savepill{min-width:150px;max-width:220px}\n  .filebar .fname{flex-basis:190px;min-width:170px;margin-inline-start:0}\n}\n`;

await writeFile(cssPath, css, "utf8");

const testPath = "test/responsive-header.test.mjs";
let test = await readFile(testPath, "utf8");
test = `import test from "node:test";\nimport assert from "node:assert/strict";\nimport { readFile } from "node:fs/promises";\n\nconst css = await readFile(new URL("../src/styles/app.css", import.meta.url), "utf8");\nconst html = await readFile(new URL("../src/index.html", import.meta.url), "utf8");\n\ntest("header keeps top-level controls compact and aligned", () => {\n  assert.match(css, /compact localized header revision/);\n  assert.match(css, /header\\{[^}]*display:flex[^}]*flex-wrap:wrap/s);\n  assert.match(css, /header>\\.brand\\{[^}]*margin-inline-end:auto/s);\n  assert.match(css, /header>button\\{[^}]*white-space:nowrap/s);\n});\n\ntest("file actions stay on one low scrollable row", () => {\n  assert.match(css, /\\.filebar\\{[^}]*flex-wrap:nowrap[^}]*overflow-x:auto[^}]*overflow-y:hidden/s);\n  assert.match(css, /\\.filebar \\.btn\\{[^}]*white-space:nowrap/s);\n  assert.match(css, /#btnEnRef,#btnSaveJson,#btnLoadJson,#btnNew,#btnExport\\{[^}]*min-width:max-content/s);\n});\n\ntest("responsive layout preserves editor height instead of stacking action buttons", () => {\n  assert.match(css, /@media \\(max-width:900px\\)[\\s\\S]*header>\\.meter\\{[^}]*flex:1 1 100%/);\n  assert.doesNotMatch(css.slice(css.lastIndexOf("compact localized header revision")), /\\.filebar\\{[^}]*flex-wrap:wrap/s);\n});\n\ntest("header keeps all existing controls and file actions", () => {\n  for (const id of ["uiLang", "meter", "savePill", "btnEnRef", "btnSaveJson", "btnLoadJson", "btnNew", "outName", "btnExport"]) {\n    assert.match(html, new RegExp(`id=["']${id}["']`));\n  }\n});\n`;
await writeFile(testPath, test, "utf8");

console.log("Applied compact non-wrapping header revision.");
